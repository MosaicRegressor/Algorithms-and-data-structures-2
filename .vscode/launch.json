{
    "version": "0.2.0",
    "configurations": [
        {
            "name":"Build, then launch with args and debug :)",
            "type":"cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.out",
            "args": ["aa", "aaaaa"],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "MIMode": "gdb",
            "externalConsole": false,
            "preLaunchTask": "Compile and debug C :3"
        }
    ]
}